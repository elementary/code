include(Translations)
add_translations_directory("scratch-text-editor")
add_translations_catalog("scratch-text-editor"
    ../src
    ../src/Widgets
    ../src/Dialogs
    ../src/Services
    ../scratchcore
    ../plugins/pastebin
    ../plugins/filemanager
    ../plugins/terminal
    ../plugins/browser-preview
    ../plugins/spell-check
    ../plugins/devhelp
)
