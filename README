Testing and installation
------------------------
$ mkdir build
$ cd build/
$ cmake -DCMAKE_INSTALL_PREFIX=/usr ..
$ make
$ sudo make install # do not worry, it's only needed for gsettings schema
$ ./scratch

Thanks!

At the moment, the installation via cmake installs only the schema.
